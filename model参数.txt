runG1:
    model.train(
        data="Data_G/data.yaml",   # 相对路径也可以
        epochs=200,
        imgsz=640,
        batch=8,
        workers=4,               # 读取数据的线程数
        patience=30,             # 若连续30次评估指标不提升，自动提前停止
        optimizer="SGD",         # 或者试试 AdamW，默认是 auto
        close_mosaic=10,         # 最后10轮关闭 mosaic 更利于模型稳定收敛
        degrees=0.0,             # 图像旋转角度范围（默认10）
        scale=0.5,               # 图像缩放（默认0.5，建议保留）
        hsv_h=0.015,             # 色调变换
        hsv_s=0.7,               # 饱和度变换
        hsv_v=0.4,               # 亮度变换
        project="runsG1",
        name="yolov11_custom",
        exist_ok=True
    )
runG2: 在G1模型基础上进行进一步训练